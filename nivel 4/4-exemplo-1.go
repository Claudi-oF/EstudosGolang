package main

import (
	"fmt"
)

func main() {
	array := [3]int{1, 2, 3}
	fmt.Println(array)

	slice := []int{1, 2, 3}
	fmt.Println(slice)

	//a diferenca do array e do slice é que o array tem um numero pre-determinado de elementos, e o slice nao tem
}
